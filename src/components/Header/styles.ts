import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#171e23',
    height: 120,
    alignItems: 'center',
  },
  text: {
    position: 'absolute',
    bottom: '25%',
    fontSize: 24,
    color: '#fff'
  }
})